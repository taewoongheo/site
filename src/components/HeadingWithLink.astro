---
interface Props {
  title: string;
  link: string;
  href: string;
}

const { title, link, href } = Astro.props;
---

<div class="flex flex-col items-start gap-1">
  <h2 class="text-3xl font-bold">{title}</h2>
  <a href={href} class="animate-underline animate-opacity">{link}</a>
</div>
